
<div class="col-md-12">
  <div class="well well-sm">

    <%= form_tag backend_media_path(@media), method: :patch do %>
      <fieldset>
        <legend class="text-center"><%= t('medias.titles.edit_practice') + " " + @media.id.to_s %></legend>

        <div class="form-group">
          <%= label_tag t('medias.attributes.title'), nil, class: "col-md-3 control-label" %>
          <div class="col-md-9">
            <%= text_field_tag :title, @media.title, class: "form-control" %>
            <div class="small-spacer"></div>
          </div>
        </div>


        <div class="form-group">
          <%= label_tag t('medias.attributes.description'), nil, class: "col-md-3 control-label" %>
          <div class="col-md-9">
            <%= text_area_tag :description, @media.description, class: "form-control", rows: 5 %>
            <div class="small-spacer"></div>
          </div>
        </div>

        <div class="form-group">
          <div class="col-md-12 text-right">
            <%= submit_tag t('medias.buttons.submit_edit'), class: "btn btn-primary btn-lg" %>
          </div>
        </div>
      </fieldset>
    <% end %>

  </div>
</div>
