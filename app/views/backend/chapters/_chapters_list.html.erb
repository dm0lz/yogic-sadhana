
<div class="container">
  <% @chapters.each do |chapter| %>
    <div class="row well well-sm">

      <div class="col-md-2">
        <%= image_tag chapter.picture, :size => "120x120" %>
      </div>

      <div class="col-md-8">

        <ul>
          <li>
            <h4><%= t('chapters.attributes.title') %></h4>
            <%= chapter.title %>
          </li>
          <li>
            <h4><%= t('chapters.attributes.description') %></h4>
            <%= chapter.description %>
          </li>

        </div>

        <div class="col-md-2">
          <li>
            <%= link_to t('chapters.buttons.show_chapter'), backend_chapter_path(chapter), class: "btn btn-success" %>
          </li>
          <div class="small-spacer"></div>
          <li>
            <%= link_to t('chapters.buttons.edit_chapter'), edit_backend_chapter_path(chapter), method: :get, class: "btn btn-info" %>
          </li>
          <div class="small-spacer"></div>
          <li>
            <%= link_to t('chapters.buttons.destroy_chapter'), backend_chapter_path(chapter), method: :delete, class: "btn btn-danger" %>
          </li>
        </ul>

        </div>

    </div>
  <% end %>
</div>
