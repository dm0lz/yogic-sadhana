

<div class="col-md-12">
  <div class="well well-sm">

    <%= form_tag backend_practice_path(@practice), method: :patch do %>
      <fieldset>
        <legend class="text-center"><%= t('practices.titles.edit_practice') + " " + @practice.id.to_s %></legend>

        <div class="form-group">
          <%= label_tag t('practices.attributes.title'), nil, class: "col-md-3 control-label" %>
          <div class="col-md-9">
            <%= text_field_tag :title, @practice.title, class: "form-control" %>
            <div class="small-spacer"></div>
          </div>
        </div>


        <div class="form-group">
          <%= label_tag t('practices.attributes.description'), nil, class: "col-md-3 control-label" %>
          <div class="col-md-9">
            <%= text_area_tag :description, @practice.description, class: "form-control", rows: 5 %>
            <div class="small-spacer"></div>
          </div>
        </div>

        <div class="form-group">
          <div class="col-md-12 text-right">
            <%= submit_tag t('practices.buttons.submit_edit'), class: "btn btn-primary btn-lg" %>
          </div>
        </div>
      </fieldset>
    <% end %>

  </div>
</div>
