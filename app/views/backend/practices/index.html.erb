

<div class="container">
  <div class="row">
    <h1>Chapter : <%= @chapter.title %></h1>
    <ul class="well">
      <li><%= t('chapters.attributes.id') %> : <%= @chapter.id %></li>
      <li><%= t('chapters.attributes.title') %> : <%= @chapter.title %></li>
      <li><%= t('chapters.attributes.description') %> : <%= @chapter.description %></li>
    </ul>
  </div>
</div>

<%= link_to t('practices.buttons.create_practice'), new_backend_chapter_practice_path, class: "btn btn-warning" %>

<div class="small-spacer"></div>

<h1><%= t('practices.buttons.list_title') %></h1>

<%= render 'backend/practices/practices_list' %>

<%= link_to t('chapters.buttons.back_to_chapter_description'), backend_chapter_path(@chapter), class: "btn btn-success" %>

<div class="small-spacer"></div>
